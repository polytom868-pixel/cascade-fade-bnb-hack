# Research Questions: CascadeFade Deep Analysis

> Generated by research sub-agent | Date: 2026-06-20
> Purpose: Identify gaps, risks, and edge cases NOT covered in ARCHITECTURE.md, PLAN.md, or SUBMISSION.md

---

## File 1: ARCHITECTURE.md — Questions (1–8)

### Question 1: What happens if TWAK `swap` command fails mid-execution after transaction submission but before confirmation?

**Priority:** CRITICAL  
**Relation:** ARCHITECTURE.md (Section 5 — Execution Layer)

**Gap:** The architecture describes `twak swap` as the primary execution surface but does not document what happens when the swap command fails after the transaction hash is returned but before confirmation. Specifically:
- What if TWAK's subprocess crashes or is killed mid-execution?
- What if the RPC returns a tx hash but the transaction reverts on-chain?
- How does the agent detect and recover from a "sent but unconfirmed" state?

**Missing from docs:** No retry logic, no idempotency key, no pending-tx tracking, no handling of `nonce too low` errors from BSC RPC.

---

### Question 2: How does SQLite behave under concurrent asyncio reads/writes during 24/7 operation, and what locking strategies are required?

**Priority:** CRITICAL  
**Relation:** ARCHITECTURE.md (Section 6 — Logging & Proof Layer)

**Gap:** The architecture specifies SQLite for the trade journal but does not address:
- Python's `sqlite3` is not thread-safe; asyncio coroutines accessing the same connection can corrupt data
- The WAL mode implications for concurrent reads during active trading
- How the agent handles `database is locked` errors during high-frequency decision cycles
- Whether `aiosqlite` is needed instead of the stdlib `sqlite3`

**Missing from docs:** No WAL mode configuration, no connection pooling, no lock timeout settings.

---

### Question 3: Does `twak swap` route through PancakeSwap v3 pools only, or does it also use v2/stable pools, and how does the agent verify which pool was used for slippage estimation?

**Priority:** HIGH  
**Relation:** ARCHITECTURE.md (Section 5 — Execution Layer)

**Gap:** The architecture mentions TWAK "may not guarantee PancakeSwap routing" but does not address:
- If TWAK uses v2 pools, the QuoterV2 slippage estimate (v3 only) will be inaccurate
- The agent cannot validate that the executed price matches the quoted price across different pool versions
- How the agent handles situations where TWAK routes through an unexpected pool, resulting in higher slippage than QuoterV2 predicted

**Missing from docs:** No verification mechanism for TWAK's actual routing path, no fallback when QuoterV2 and TWAK quotes diverge.

---

### Question 4: What constitutes a qualifying "trade" for the competition — does a failed swap (reverted transaction) count, and how are micro-swap heartbeat trades judged?

**Priority:** CRITICAL  
**Relation:** ARCHITECTURE.md (Section 1 — System Overview)

**Gap:** The architecture mentions "at least one qualified trade per day" but does not address:
- If a heartbeat swap executes but the output amount is $0.01 due to gas costs, does it count?
- Are reverted transactions (failed due to slippage, nonce, or insufficient gas) counted or penalized?
- Does the competition count internal transfers (e.g., USDT → BNB within the same wallet) as trades?
- What is the minimum USD value of a trade that judges will recognize as valid?

**Missing from docs:** No definition of "qualified trade," no handling of dust amounts, no verification that heartbeat trades are recognized as valid.

---

### Question 5: How does the agent handle BSC network congestion — specifically, what happens when gas fees spike above the estimated limit mid-execution?

**Priority:** HIGH  
**Relation:** ARCHITECTURE.md (Section 5 — Execution Layer)

**Gap:** The architecture uses PancakeSwap MEV Guard RPC but does not address:
- What gas price ceiling the agent should set to prevent runaway fees
- How to handle transactions stuck in the mempool for >5 minutes during congestion
- Whether the agent should use EIP-1559 type 2 transactions with a gas premium cap
- What the retry strategy is when a transaction is pending but not confirmed after 10+ minutes

**Missing from docs:** No gas estimation strategy, no pending tx timeout handling, no gas price caps.

---

### Question 6: Is the 149-token allowlist provided by the competition organizers in machine-readable format, and what happens if one token from the list is not tradeable on PancakeSwap?

**Priority:** HIGH  
**Relation:** ARCHITECTURE.md (Section 4 — Decision Layer, Section 7 — Risk Management)

**Gap:** The architecture hardcodes the 149-token allowlist but does not address:
- Whether the list was obtained from the competition contract or from a web page
- If a token from the list has zero liquidity on PancakeSwap, what happens when the signal triggers a buy?
- Does the agent have a "liquidity check" before executing a buy, or does it rely on TWAK/slippage estimation?
- What is the fallback if the QuoterV2 returns zero output for a valid allowlist token?

**Missing from docs:** No liquidity pre-check, no handling of zero-liquidity tokens, no validation that all 149 tokens are actually tradeable.

---

### Question 7: How do judges verify PnL — do they use BSCScan's native portfolio tracking, manual CSV export, or a custom scoring contract, and what format must the agent's SQLite export match?

**Priority:** HIGH  
**Relation:** ARCHITECTURE.md (Section 6 — Logging & Proof Layer)

**Gap:** The architecture claims BSCScan is the "canonical proof" but does not address:
- Does BSCScan automatically track USD-denominated PnL for a wallet, or do judges manually compute it?
- Does the competition provide a scoring contract or script that judges run against registered wallets?
- If judges use a different exchange rate source (e.g., CoinGecko vs CMC), will the PnL calculation differ?
- Is there an official JSON/C SV export format judges expect, or is it purely BSCScan-based?

**Missing from docs:** No description of judges' actual verification workflow, no standardization of PnL calculation methodology.

---

### Question 8: Does TWAK's `twak serve` expose a REST/MCP interface that survives long-running operation, and are there known stability issues with the stdio transport?

**Priority:** MEDIUM  
**Relation:** ARCHITECTURE.md (Section 5 — Execution Layer)

**Gap:** The architecture mentions `twak serve` as optional but does not address:
- Whether `twak serve` running in stdio mode will leak memory over 7 days
- How the agent handles a broken stdio pipe to `twak serve` mid-loop
- Whether the REST mode of `twak serve` is stable enough for continuous production use
- What the authentication mechanism is for `twak serve` REST endpoints

**Missing from docs:** No stability testing, no reconnection strategy, no authentication details for `twak serve`.

---

## File 2: PLAN.md — Questions (1–8)

### Question 1: What is the exact retry/backoff logic when CMC returns a 429 (rate limit) error — does the agent skip the entire trading cycle or use stale cached data?

**Priority:** CRITICAL  
**Relation:** PLAN.md (Phase 1, Task 1.2 — CMC REST client)

**Gap:** The plan specifies implementing a CMC client with "rate-limit handling" but does not define:
- The exponential backoff parameters (max retries, initial delay, jitter)
- Whether the agent falls back to cached data or halts all trading during rate limiting
- What happens if the cache is empty and CMC returns 429 — does the agent proceed on stale data from a previous cycle?
- Is there a distinction between rate limits for free-tier vs API-key-authenticated requests?

**Missing from docs:** No retry policy, no stale-data fallback behavior, no rate-limit budget tracking across cycles.

---

### Question 2: Can the `twak compete register` transaction be replaced if it fails, or does the wallet address need to match exactly what was submitted on the DoraHacks form?

**Priority:** CRITICAL  
**Relation:** PLAN.md (Phase 0, Task 0.8 — Registration)

**Gap:** The plan says registration must be completed before June 22 but does not address:
- What happens if the `twak compete register` transaction reverts or gets stuck
- Whether re-registering the same wallet is idempotent or creates duplicate entries
- If the operator creates a second wallet after submission, can they update the DoraHacks form?
- Does the competition contract allow re-registration if the first tx failed?

**Missing from docs:** No registration failure handling, no update mechanism, no idempotency guarantee for `twak compete register`.

---

### Question 3: Does the 25% drawdown kill switch measure portfolio value in USD using real-time CMC prices, and what happens when CMC data is stale or unavailable during a sharp market move?

**Priority:** CRITICAL  
**Relation:** PLAN.md (Phase 4, Task 4.3 — Drawdown check)

**Gap:** The plan tests the drawdown kill at 25% but does not address:
- If CMC returns stale prices during a flash crash, the agent may not detect the drawdown in time
- Does the agent use TWAK wallet balance (on-chain) for portfolio valuation instead of or in addition to CMC?
- What is the USD conversion source for on-chain token balances — CMC at signal time or at settlement time?
- If the portfolio briefly dips below 25% due to CMC lag but recovers within minutes, does the kill switch fire irreversibly?

**Missing from docs:** No price source for portfolio valuation, no stale-price detection, no on-chain fallback for valuation.

---

### Question 4: Does the agent use systemd or tmux/screen for 24/7 operation, and does systemd's restart policy introduce race conditions during crashes?

**Priority:** HIGH  
**Relation:** PLAN.md (Phase 5, Task 5.6 — run.sh script)

**Gap:** The plan mentions `run.sh` using `tmux` or `nohup` but does not address:
- tmux/screen sessions can be killed by OS reboots or SSH session timeouts without the agent being restarted
- systemd is more resilient for 24/7 but requires root and has restart delay intervals that could cause missed heartbeat trades
- If the agent crashes and systemd restarts it immediately, does this cause nonce conflicts with pending TWAK transactions?
- What is the recovery strategy when the agent restarts and finds open/pending positions from the previous session?

**Missing from docs:** No process supervision strategy, no crash recovery, no pending-transaction cleanup after restart.

---

### Question 5: How does the agent handle BNB gas estimation — specifically, what happens if gas fees exceed the estimated cap and the transaction fails due to "out of gas"?

**Priority:** HIGH  
**Relation:** PLAN.md (Phase 3, Task 3.3 — QuoterV2 slippage estimation)

**Gap:** The plan focuses on slippage estimation via QuoterV2 but does not address:
- BSC gas estimation for complex PancakeSwap v3 routing (which uses more gas than simple transfers)
- What happens if the agent sets a fixed gas limit that is too low for multi-hop swaps
- Whether TWAK handles gas estimation automatically, or if the agent needs to set `--gas-limit`
- What the fallback is when gas fees on BSC spike above a threshold (e.g., >5 gwei) during the trading window

**Missing from docs:** No gas limit configuration, no gas price spike handling, no TWAK gas estimation behavior documented.

---

### Question 6: Are there any token pairs in the 149-token list where USDT is not available as a trading pair on PancakeSwap, forcing the agent to use BNB or BUSD as an intermediary?

**Priority:** HIGH  
**Relation:** PLAN.md (Phase 2, Task 2.2 — Buy rule)

**Gap:** The plan uses USDT for the heartbeat trade but does not address:
- If a token can only be swapped against BNB or BUSD (not USDT directly), the agent needs a 2-hop route
- Does TWAK support multi-hop routing, or does the agent need to decompose it into two swaps?
- Does using BNB as an intermediary introduce additional price risk during the heartbeat trade?
- Is BUSD still considered a stablecoin on BSC after the BUSD deprecation/discontinuation, and is it on the 149 list?

**Missing from docs:** No validation of trading pair availability, no handling of 2-hop routes, no clarification on BUSD status.

---

### Question 7: What happens if the CMC API key is invalidated, rotated, or exceeds the 50 req/min limit during a high-volatility period — does the agent stop trading or continue blindly?

**Priority:** HIGH  
**Relation:** PLAN.md (Phase 1, Task 1.2 — CMC client)

**Gap:** The plan mentions the Basic tier has "15,000 credits/month, 50 req/min" but does not address:
- If the API key is compromised or rate-limited during the 7-day trading window, is there a backup key strategy?
- Does the agent have a "safe mode" where it only does heartbeat trades when CMC is unreachable?
- What happens when 15,000 credits are exhausted mid-competition — does the agent gracefully degrade or crash?
- Is there any way to monitor credit usage proactively to avoid running out during the trading window?

**Missing from docs:** No API key backup strategy, no credit budget monitoring, no graceful degradation plan.

---

### Question 8: How does the agent handle nonce management when multiple swaps are queued or when the process restarts — are nonce gaps or collisions possible?

**Priority:** MEDIUM  
**Relation:** PLAN.md (Phase 3, Task 3.2 — Execute swap)

**Gap:** The plan mentions TWAK subprocess calls but does not address:
- If the agent queues multiple trades within a short window, TWAK may use the same nonce for concurrent transactions
- When the agent restarts, does TWAK re-sync the nonce from the BSC network, or could it reuse a nonce from a pending tx?
- What happens if a transaction is pending and the agent tries to submit the same trade again — does TWAK handle idempotency?
- Are there race conditions between TWAK's internal nonce tracking and the BSC node's nonce?

**Missing from docs:** No nonce management strategy, no idempotency handling, no restart nonce sync.

---

## File 3: SUBMISSION.md — Questions (1–8)

### Question 1: How do judges interpret "total return" — is it calculated from the initial wallet balance, from the USDT entry cost basis, or from a combination of entry and exit prices over all trades?

**Priority:** CRITICAL  
**Relation:** SUBMISSION.md (Section — Official Rules & How CascadeFade Satisfies Them)

**Gap:** The submission claims judges rank by "total return" but does not address:
- If the wallet started with 500 USDT and ends with 400 USDT + 0.1 BNB worth $150, is total return +5% or -20%?
- Does the initial balance include the BNB reserved for gas (which is not invested but still in the wallet)?
- Are unrealized gains on open positions counted, or only realized PnL?
- Is there a benchmark comparison (e.g., BNB buy-and-hold), or is it pure absolute return?

**Missing from docs:** No PnL calculation methodology, no unrealized vs realized treatment, no gas cost accounting in PnL.

---

### Question 2: Does the competition penalize drawdown events that occur due to network failures (e.g., the agent couldn't execute a stop-loss because RPC was down)?

**Priority:** CRITICAL  
**Relation:** SUBMISSION.md (Section — Official Rules & How CascadeFade Satisfies Them)

**Gap:** The submission claims a 25% internal drawdown stop but does not address:
- If a network outage prevents the agent from executing a sell during a drawdown event, does the competition hold the agent responsible for exceeding the 30% cap?
- Is the drawdown cap enforced by a smart contract (automatic liquidation) or by judge discretion?
- What evidence does the agent need to prove the drawdown was due to external factors (RPC failure) rather than bad strategy?
- Does the competition have a "force majeure" clause for infrastructure failures?

**Missing from docs:** No network failure handling in drawdown context, no penalty clarification, no force majeure clause.

---

### Question 3: The special prize scoring for TWAK integration uses "subjective" criteria (originality, demo). What evidence do judges need to score 10/10 on originality vs. 5/10?

**Priority:** HIGH  
**Relation:** SUBMISSION.md (Section — Best Use of Trust Wallet Agent Kit)

**Gap:** The submission scores 10/10 on originality based on the "buy calm, sell hype" thesis but does not address:
- Are judges evaluating originality against other Track 1 competitors or against all hackathon submissions?
- Does a text-based thesis score lower than a live demo of novel TWAK features (e.g., `twak serve` MCP, x402)?
- Is there a minimum demo quality or code complexity threshold for 10/10 originality?
- How much does the demo video quality (production value, clarity) influence subjective scoring?

**Missing from docs:** No guidance on what makes a 10/10 demo vs 5/10, no clarity on judge rubric for originality.

---

### Question 4: If the agent's x402 implementation fails (e.g., Base USDC wallet unfunded, Base network congestion), does it hurt the TWAK special prize score, or is it optional?

**Priority:** HIGH  
**Relation:** SUBMISSION.md (Section — Best Use of Trust Wallet Agent Kit)

**Gap:** The submission lists x402 as 10/10 of the 100-point TWAK score but marks it "optional" elsewhere. This contradiction creates risk:
- If x402 is marked as "optional" but scores 10 points, skipping it guarantees ≤90/100
- If the x402 implementation fails during the demo video, will judges mark it as incomplete?
- Does the x402 need to actually return real CMC data, or does a failed demo count?
- Is there a fallback x402 demo (e.g., using testnet USDC) that still scores points?

**Missing from docs:** No clarification on whether x402 is truly optional or required for the special prize, no fallback plan.

---

### Question 5: The BNB AI Agent SDK section mentions "MegaFuel gas-free registration is testnet-only" but the architecture shows mainnet ERC-8004. What happens on mainnet registration when gas is required?

**Priority:** HIGH  
**Relation:** SUBMISSION.md (Section — Best Use of BNB AI Agent SDK)

**Gap:** The submission claims ERC-8004 identity for BNB AI Agent SDK special prize but does not address:
- If MegaFuel only sponsors testnet gas, how does the agent pay for mainnet ERC-8004 registration?
- Does the TWAK wallet need to have extra BNB (beyond trading capital) for ERC-8004 registration?
- What is the estimated gas cost of ERC-8004 mainnet registration in USD?
- If mainnet ERC-8004 fails due to insufficient gas, does the agent fall back to testnet registration, and does testnet count?

**Missing from docs:** No mainnet gas estimation for ERC-8004, no MegaFuel mainnet sponsorship clarity, no fallback plan.

---

### Question 6: Does the demo video need to be a live recording, or can it be a screen recording? And does a screen recording with simulated data score lower than a live mainnet demo?

**Priority:** MEDIUM  
**Relation:** SUBMISSION.md (Section — Demo Video Plan)

**Gap:** The submission plans a 3-minute walkthrough but does not address:
- If the live demo fails (e.g., no signal fires, no trade executes during recording), can the video be edited or must it be continuous?
- Are judges able to verify that the transactions in the video are on-chain and not simulated?
- Does the demo need to show a profitable trade, or can it show the risk guardrails firing (a stopped trade)?
- Is there a minimum time window for the demo to be considered valid?

**Missing from docs:** No demo format requirements, no verification of demo authenticity, no minimum content requirements.

---

### Question 7: What happens if a token from the 149-token list is delisted, paused, or flagged as a scam during the trading window — does the agent have any detection mechanism?

**Priority:** HIGH  
**Relation:** SUBMISSION.md (Section — Official Rules)

**Gap:** The submission relies on the hardcoded 149-token allowlist but does not address:
- If a token is flagged as a scam post-submission, does trading it disqualify the agent?
- Does the agent monitor for pausable tokens or admin key risks?
- What is the agent's response if a held token's contract is upgraded or paused by the team?
- Does the competition provide an updated allowlist during the trading window, or is the initial list fixed?

**Missing from docs:** No token safety monitoring, no contract upgrade detection, no response plan for compromised tokens.

---

### Question 8: Are there any tax, regulatory, or compliance implications of running an autonomous trading agent on BSC that judges or the competition organizers may flag?

**Priority:** MEDIUM  
**Relation:** SUBMISSION.md (Section — Final Note to Judges)

**Gap:** The submission focuses on technical and trading aspects but does not address:
- Some jurisdictions may require KYC/AML for automated trading bots; does the competition require any KYC?
- If the agent's trading pattern is flagged by BSC for wash trading or market manipulation, can the competition disqualify the team?
- Is there a minimum trade size below which trades are considered "artificial" (e.g., the $5 heartbeat)?
- Does the agent need to include any legal disclaimer in the submission or README?

**Missing from docs:** No compliance guidance, no wash trading risk assessment, no legal disclaimer requirement.

---

## Summary: Priority Matrix

| # | Question | File | Priority |
|---|---|---|---|
| 1 | TWAK swap failure mid-execution | ARCHITECTURE | CRITICAL |
| 2 | SQLite locking in asyncio | ARCHITECTURE | CRITICAL |
| 3 | TWAK routing verification | ARCHITECTURE | HIGH |
| 4 | What constitutes a "trade" | ARCHITECTURE | CRITICAL |
| 5 | BSC network congestion handling | ARCHITECTURE | HIGH |
| 6 | 149-token PancakeSwap liquidity | ARCHITECTURE | HIGH |
| 7 | Judges' PnL verification method | ARCHITECTURE | HIGH |
| 8 | TWAK serve stability | ARCHITECTURE | MEDIUM |
| 9 | CMC 429 retry/backoff logic | PLAN | CRITICAL |
| 10 | twak compete register failure | PLAN | CRITICAL |
| 11 | Drawdown calculation with stale CMC | PLAN | CRITICAL |
| 12 | systemd vs tmux for 24/7 | PLAN | HIGH |
| 13 | BSC gas estimation for swaps | PLAN | HIGH |
| 14 | USDT pair availability in 149 list | PLAN | HIGH |
| 15 | CMC API key failure strategy | PLAN | HIGH |
| 16 | Nonce management on restart | PLAN | MEDIUM |
| 17 | Total return calculation methodology | SUBMISSION | CRITICAL |
| 18 | Drawdown penalty for network failures | SUBMISSION | CRITICAL |
| 19 | TWAK originality scoring rubric | SUBMISSION | HIGH |
| 20 | x402 optional vs required contradiction | SUBMISSION | HIGH |
| 21 | ERC-8004 mainnet gas cost | SUBMISSION | HIGH |
| 22 | Demo video format requirements | SUBMISSION | MEDIUM |
| 23 | Token delist/pause detection | SUBMISSION | HIGH |
| 24 | Tax/compliance implications | SUBMISSION | MEDIUM |

---

*Document generated by research sub-agent. Questions are ordered by priority within each file section.*
